'use client';
import LuxuryHeroButton from '@/features/shared/components/ui/LuxuryHeroButton';
import { VedicMandalaPremium } from './VedicMandalaPremium';
import { CommunityCounter3D } from './CommunityCounter3D';

const heroData = {
  title: 'Ancient Wisdom.',
  subtitle: 'Modern Precision.',
  description: 'Guided by Acharya KK Nagaich ji, rooted in the sacred geometry of Uttar Pradesh.',
  button_text: 'Consult Acharya',
  button_link: '/contact',
  secondary_button_text: 'Explore Free Tools',
  secondary_button_link: '/free-tools',
};

export function CosmicHero() {
  return (
    <section className="relative min-h-screen flex flex-col items-center justify-center overflow-hidden">
      <div className="absolute inset-0 bg-gradient-to-br from-nidra-indigo via-sacred-saffron/10 to-kumkuma-red/15" />
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_center,transparent_0%,#1A2A3A_70%)] opacity-40" />
      <VedicMandalaPremium />

      <div className="container mx-auto px-4 sm:px-6 relative z-10 text-center mt-20 animate-fadeIn">
        <h1 className="font-serif text-4xl sm:text-5xl md:text-7xl lg:text-8xl text-white mb-6 leading-tight drop-shadow-2xl">
          <span className="bg-gradient-to-r from-sacred-saffron via-prakash-gold to-kumkuma-red bg-clip-text text-transparent">
            {heroData.title}
          </span>
          <br />
          <span className="text-prakash-gold">{heroData.subtitle}</span>
        </h1>
        <p className="font-sans text-base sm:text-lg md:text-xl text-white/80 max-w-2xl mx-auto mb-10 px-4">
          {heroData.description}
        </p>
        <div className="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12">
          <LuxuryHeroButton href={heroData.button_link} variant="primary">
            {heroData.button_text}
          </LuxuryHeroButton>
          <LuxuryHeroButton href={heroData.secondary_button_link} variant="secondary">
            {heroData.secondary_button_text}
          </LuxuryHeroButton>
        </div>
        <CommunityCounter3D />
      </div>

      <div className="absolute bottom-8 left-1/2 -translate-x-1/2">
        <span className="block w-6 h-10 border-2 border-prakash-gold rounded-full mx-auto">
          <span className="block w-1 h-3 bg-prakash-gold rounded-full mx-auto mt-2 animate-bounce" />
        </span>
      </div>
    </section>
  );
}
export default CosmicHero;
