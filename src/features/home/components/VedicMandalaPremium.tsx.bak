'use client';

export function VedicMandalaPremium() {
  return (
    <div className="absolute inset-0 flex items-center justify-center pointer-events-none overflow-hidden">
      <div className="relative w-[min(90vw,800px)] h-[min(90vw,800px)]">
        <style dangerouslySetInnerHTML={{ __html: `
          @keyframes spin-slow {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
          }
          @keyframes spin-reverse {
            from { transform: rotate(360deg); }
            to { transform: rotate(0deg); }
          }
          .mandala-ring-1 { animation: spin-slow 30s linear infinite; }
          .mandala-ring-2 { animation: spin-reverse 25s linear infinite; }
          .mandala-core { animation: pulse 3s ease-in-out infinite; }
          @keyframes pulse {
            0%, 100% { opacity: 0.6; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.02); }
          }
        `}} />
        <div className="mandala-ring-1 absolute inset-0 rounded-full border border-prakash-gold/20" />
        <div className="mandala-ring-2 absolute inset-4 rounded-full border border-dashed border-sacred-saffron/15" />
        <div className="mandala-core absolute inset-0 flex items-center justify-center">
          <span className="text-7xl md:text-8xl font-serif text-prakash-gold drop-shadow-2xl"
            style={{ textShadow: '0 0 30px #FF9933, 0 0 60px #C10000' }}>
            ॐ
          </span>
        </div>
      </div>
    </div>
  );
}
